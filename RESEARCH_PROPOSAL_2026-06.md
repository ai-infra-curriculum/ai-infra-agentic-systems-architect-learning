# Curriculum proposal: Agentic Systems Architect — 2026-06

Generated by `aicg org research --apply`. Caps:

- `max_modules_per_run`: 1
- `max_exercises_per_run`: 3
- `max_projects_per_run`: 0
- `min_evidence_count`: 3 job postings per item

## Rationale

Cycle 2 of research expanded the sample from the single Imagine Learning seed to 22 in-window postings (Mar 17 - Jun 15 2026) plus 5 older corroborating ones, drawn from consulting/SI architects (Cognizant, Capgemini, Infosys, EY, Guidehouse, NTT DATA, WGA, dentsu, Granicus), enterprise-platform architects (84.51/Kroger, UnitedHealth, Acquia, Experian, Vertex, Apollo, Zoom, HERE Technologies), and security/governance architects (Zscaler, Imagine Learning). Every theme that crossed the 0.30 frequency threshold maps cleanly to an existing module (multi-agent orchestration -> mod-302; memory/RAG -> mod-303; evaluation -> mod-304; observability -> mod-305; security/sandboxing/OAuth/RBAC -> mod-306; governance + ISO-42001 + regulated domains -> mod-309; MCP/plugin platforms/SDLC -> mod-310; cost economics -> mod-307; durable execution + HITL -> mod-308). The one borderline candidate -- 'MCP server / tool registry / agent gateway as a governance pattern' at 6/22 = 27% -- sits just below the threshold and is already implicitly addressed by the combination of mod-306 (OAuth/RBAC for toolchains), mod-309 (plugin lifecycle governance), and mod-310 (plugin architecture incl. MCP). Per the continuity-bias rule, the expected output is empty arrays. The 2026-06-14 seed's REQ-ASA-16 (MCP) has been internally promoted from 'preferred' to required-tier in job-requirements.json, but no curriculum change is needed because mod-302/mod-306/mod-310 already cover MCP at architect altitude. No additions, modules, exercises, or projects are proposed this cycle.

## Accepted

### Modules

_None._

### Exercises

_None._

### Projects

_None._

## Rejected

_None._

---

## Approval

**This proposal does NOT add anything to the curriculum automatically.**

If you approve some/all of the accepted items, merge this PR. After merge,
run `aicg org promote-plan --role <role>` (or wait for the next weekly
audit) to apply the filtered delta to `curriculum-plan.json` and scaffold
skeletons.

If you reject everything, close the PR. No further action needed; the
next research run will repropose anything still warranted by the job
market.
